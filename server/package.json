{
  "name": "public-risk-radar-server",
  "version": "0.1.0",
  "description": "Public Risk Radar API — address-based public risk aggregator",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "REDIS_URL= AIRNOW_API_KEY=test-key node --test",
    "test:watch": "REDIS_URL= AIRNOW_API_KEY=test-key node --test --watch"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "pg": "^8.12.0",
    "redis": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
