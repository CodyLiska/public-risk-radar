{
  "name": "public-risk-radar",
  "version": "0.1.0",
  "private": true,
  "description": "Address-based public risk aggregator — root scripts for the server + client workspaces.",
  "scripts": {
    "dev": "docker compose up -d --wait db redis && concurrently -n server,client -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "test": "npm run test:server && npm run test:client",
    "test:server": "cd server && npm test",
    "test:client": "cd client && npm test"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
